# 避難所検索結果表示画面を実装するための一括指示プロンプト

以下の指示を一度に読み込み、既存システムの避難所検索結果画面を実装してください。

## 目的

既存システムの「避難所検索結果表示画面」の部分だけを実装する。既存のヘッダー、フッター、認証、データ登録機能、検索入力画面など、今回指定していない機能や画面のデザイン・動作は変更しないこと。

既存システムのフレームワーク、テンプレートエンジン、ルーティング方式を優先して使用すること。新しいフレームワークへの移行や大規模なリファクタリングは行わないこと。

## 実装対象画面

ページの見出しは「避難所検索結果表示画面」とする。

画面上部に、次の注意喚起を表示する。

> 現在地から近い避難所はこちらです。速やかに避難をお願いします。

注意喚起は、薄いグレーの背景、濃いグレーの1px枠線、12px程度の上下余白、16px程度の左右余白、18px程度の太字で表示する。

## レイアウト

### 1. 現在地に近い避難所のメイン領域

注意喚起の下に、太さ2px程度の濃い枠線で囲んだ白い領域を配置する。

この領域はデスクトップでは左右2カラムにする。

- 左カラム: 地図
- 右カラム: 現在地に近い避難所の一覧

左右の比率はおおむね 1 : 1.4 とし、間隔は16px程度にする。領域内側の余白は22px程度にする。

右カラムの見出しは「現在地に近い避難所」とし、薄いグレーの背景、1pxの枠線、12px程度の余白で表示する。

### 2. 地図

Leaflet.js を使用して地図を表示する。CDNから Leaflet 1.9.4 を読み込んでよい。

地図の要件:

- 高さはデスクトップで340px程度、モバイルで260px程度
- 地図自体に1pxの枠線を付ける
- 現在地を青い円形マーカーで表示する
- 上位3件の避難所を地図上にピンで表示する
- ピンをクリックすると、避難所名と住所を含むポップアップを表示する
- 現在地をクリックすると「現在地」と表示する
- 地図タイルは OpenStreetMap 系の公開タイルを使用し、必ず適切な attribution を表示する

現在地のデータは固定値にせず、既存システムが渡す現在地データを使用する。現在のサンプル値は次のとおり。

```json
{
  "address": "青森県青森市古川3丁目",
  "lat": 40.8223,
  "lng": 140.7224
}
```

### 3. 上位3件の避難所カード

避難所データを現在地から近い順に並べ、先頭3件を上位避難所として表示する。カードは1件ずつ縦に並べる。

各カードは次の構成にする。

- 左側: 避難所名、ルートリンク、住所
- 右側: 混雑度、利用可能な設備アイコン

カードの仕様:

- 白い背景
- 1pxのグレー枠線
- 最小高さは95px程度
- 左右2カラム
- 避難所名は20px程度、通常ウェイト
- ルートリンクの表示文字は「マップのリンク」
- ルートリンクは Google Maps 検索URLにし、別タブで開く
- URLの検索語には避難所名を使用する
- 住所は「住所：」の後に表示する

混雑度は「混雑度」の下に表示し、次の色分けを行う。

- 「空きあり」または「空き有り」: 緑
- 「混雑」を含む値: オレンジ
- 「満員」: 赤
- その他: 通常の文字色

設備フラグが true の場合だけ、次のアイコンとラベルを表示する。設備アイコンは1行に収め、中央揃えにする。

| データ項目 | アイコン | ラベル |
|---|---|---|
| `wheelchair` | ♿ | 車椅子 |
| `pet` | 🐾 | ペットOK |
| `help` | 🆘 | 要配慮 |
| `medical` | 🩹 | 医療 |

絵文字が利用できない環境では、既存システムのアイコンライブラリまたは意味が同じ代替アイコンを使用してよい。ただし、アイコンとラベルの意味は変えないこと。

### 4. その他の避難所

上位3件の下に、別の白い領域を配置する。ここにも太さ2px程度の濃い枠線を使用し、内側の余白は22px程度にする。

見出しは「その他の避難所はこちら」とする。

4件目以降の避難所を、各1行の横長レイアウトで表示する。各行には次の情報を含める。

- 順位と避難所名
- 「ルート：マップのリンク」という Google Maps 別タブリンク
- 「住所：」と住所
- 「混雑度：」と色分け済みの混雑度
- true の設備フラグに対応するアイコンとラベル

横幅が不足する場合は自然に折り返す。デスクトップでは設備アイコンを行の右側に寄せ、モバイルでは無理に横一列に固定せず、読みやすく折り返す。

4件目以降が存在しない場合は、「4番目以降の避難所はありません。」と表示する。

## データ契約

画面は、最低限次の配列を受け取れるようにする。

```json
[
  {
    "id": 1,
    "name": "青森市立古川小学校",
    "address": "青森県青森市古川3丁目7-14",
    "lat": 40.8223,
    "lng": 140.7343,
    "congestion": "空きあり",
    "wheelchair": true,
    "pet": true,
    "help": true,
    "medical": false
  }
]
```

バックエンド側で次を保証すること。

1. 現在地からの距離が近い順に並べてから画面へ渡す。
2. 地図に使う `lat` と `lng` が存在する避難所だけをマーカー対象にする。
3. データが空でもテンプレートや JavaScript がエラーにならないようにする。
4. テンプレートから JavaScript に渡すデータは、文字列連結ではなく JSON として安全に埋め込む。
5. 避難所名や住所などの表示文字列は、テンプレートエンジンの通常のエスケープを有効にする。

現在地からの距離計算は、既存システムに距離計算処理がある場合はそれを再利用する。ない場合は、近距離の並び替えに適した簡易的な緯度・経度距離でよい。

## 画面下部の操作

画面下部に次のリンクを横並びで配置する。

- 「検索に戻る」: 避難所検索画面へ戻る
- 「トップページへ戻る」: トップページへ戻る

リンクは淡い水色の背景、薄い青灰色の枠線、中央揃え、最小幅145px程度とする。ホバー時は少し濃い水色に変える。モバイルでは必要に応じて折り返す。

## レスポンシブ対応

768px以下では次のようにする。

- 地図と避難所一覧の2カラムを1カラムに変更する
- 地図の高さを260px程度にする
- 外側の余白を16px程度に縮める
- 上位カードの右側情報は幅220px程度を確保する
- その他の避難所の行は折り返し可能にする
- 下部リンクは折り返し可能にする
- テキスト、アイコン、リンクが親要素からはみ出したり重なったりしないようにする

## 実装上の注意

- この画面に必要なテンプレート、スタイル、地図初期化処理、データ受け渡し、ルートだけを変更する。
- 既存の共通レイアウトのスタイルを壊さず、画面固有のスタイルには画面専用のクラス名を付ける。
- 既存のURL構成が異なる場合は、実際の既存ルートに合わせてリンク先を置き換える。
- 検索結果が0件の場合も、空の画面や JavaScript エラーにせず、利用者に状況が分かる表示を出す。
- 外部リンクは `target="_blank"` と適切な `rel="noopener noreferrer"` を付ける。
- 地図ライブラリの読み込み失敗があっても、避難所一覧自体は表示できる構造にする。
- 既存の日本語フォント、文字コード、テンプレート記法に合わせる。

## 完了条件

次の条件をすべて満たすまで実装すること。

- ページタイトルと注意喚起が表示される
- 現在地を中心とした地図が表示される
- 現在地と上位3件の避難所が地図上に表示される
- 避難所が距離順に並び、上位3件と4件目以降に分かれて表示される
- 避難所名、住所、Google Mapsリンク、混雑度、設備アイコンが正しく表示される
- 混雑度の色分けが機能する
- データが0件でも画面が壊れない
- 768px以下で1カラムになり、内容が重ならない
- 「検索に戻る」「トップページへ戻る」が機能する
- 既存の対象外画面の表示や機能に不要な変更がない

実装後、利用可能なテスト、テンプレート検証、起動確認のいずれかを実行し、エラーがあれば修正すること。最後に変更したファイルと確認内容を簡潔に報告すること。